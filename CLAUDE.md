# Instructions projet

Ce fichier n'existe que parce que Claude Code charge ses instructions depuis la racine du
dépôt : c'est de la configuration d'outil, au même titre que le `Makefile` ou le
`.gitignore`. La documentation, elle, vit entièrement dans `docs/`.

@docs/CONVENTIONS.md
